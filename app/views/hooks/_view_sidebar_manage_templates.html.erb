<% if User.current.admin? || User.current.allowed_to?(:create_issue_templates, @project) %>
  <h3><%= l(:label_issue_templates) %></h3>
  <%= link_to l("show_templates"), "/issue_templates?project_id=#{@project.try(:id)}" %>
<% end %>
