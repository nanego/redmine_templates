<%= issue_template_section_form(form, IssueTemplateSectionDate, template) do %>
  <p>
    <%= form.text_field :title, :placeholder => "Titre", :label => 'Date', :size => 45, :required => true %>
  </p>
  <div class="collapsible">
    <%= render partial: "issue_templates/sections/section_id", locals: {section: form.object} %>
    <p>
      <%= form.text_field :description, :placeholder => "Description", label: "Description", :size => 80 %>
    </p>
    <p>
      <%= form.check_box :required, label: :label_field_required %>
    </p>
    <p>
      <%= form.select :select_type,
                      options_for_select(IssueTemplateSectionDate.select_types_options,
                                         selected: (form.object.present? ? form.object.select_type : '')),
                      label: "Format" %>
    </p>
    <p>
      <%= form.text_field :empty_value, label: "Valeur par défaut", :size => 80 %>
    </p>
  </div>
<% end %>
